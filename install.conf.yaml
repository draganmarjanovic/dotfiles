- defaults:
    link:
      relink: true

- clean: []

- link:
    ~/.zshrc: shell/zshrc
    ~/.oh-my-zsh/themes/honukai.zsh_theme: shell/honukai.zsh-theme

- shell:
    - [source ~/.zshrc]
    - [cp -n shell/extended_config ~/.extended_config]
